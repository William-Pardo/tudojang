// Fixture: sin `soporteMeta` -- su cobertura viene de una entrada MANUAL en
// shared/soporte/catalogo.v1.ts de este mismo fixture (analogo a cualquier vista aun no
// migrada, en la linea base de deuda congelada que consume el gate).
import React from 'react';

const VistaCubierta: React.FC = () => <div>Vista cubierta por entrada manual</div>;

export default VistaCubierta;
