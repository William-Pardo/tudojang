// Stub agregado en la Fase 2 (scripts/rutas-app.test.js): leerRutasApp() resuelve imports
// relativos via existsSync, asi que App.tsx necesita que estos archivos existan de verdad en
// disco. Sin logica real: solo lo minimo para que la resolucion de imports encuentre un
// archivo .tsx valido.
import React from 'react';

const VistaSimple: React.FC = () => <div>Vista simple (stub de app-rutas)</div>;

export default VistaSimple;
