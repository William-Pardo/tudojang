# Tasks: Unificar Clase en Vivo sobre JornadaInstruccion, disparada por Agenda (Bloque A) + funcionalidad completa (Bloque B)

**Rehecho de cero** (intento anterior se cortó por límite de sesión, cubría solo Fases 0-6 con ventana 5/10 desactualizada). Esta versión corrige la ventana a 15/15 anclada a `horaFin` (ver `design.md`, nota de cabecera) y agrega Bloque B completo (Fases 7-15).

## Orden de bloqueo (explícito)

- Fase 0 es prerrequisito de **todas** las fases siguientes: sin roster no hay contra qué validar pertenencia.
- Fases 1-6 son secuenciales entre sí (cada una bloquea la próxima, ver encabezado de cada fase) — Bloque A completo.
- **GATE bloqueante Bloque A → Bloque B**: la Fase 7 (y por transitividad 8-15) NO arranca hasta que Fases 0-6 estén implementadas y `sdd-verify` confirme 0 regresiones. Construir Bloque B sobre un Bloque A no verificado reproduciría el problema original de conexiones rotas/múltiples fuentes de verdad.
- Dentro de Bloque B: Fase 7 bloquea 8; Fase 8 bloquea 9, 10, 11 y 12 (estas tres corren en paralelo entre sí, todas solo requieren Fase 8 verde); Fase 13 requiere 9+10+11+12 verdes (ensambla todo el flujo visual); Fase 14 requiere 13; Fase 15 requiere 14.

## Phase 0: Roster explícito de matrícula (BLOQUEANTE — ninguna tarea de Fase 1-15 puede iniciar sin Fase 0 completa y verde)

**Nota de auditoría (esta revisión)**: un intento previo de `sdd-apply` se cortó por límite de sesión de la API a mitad de una técnica RED válida — había removido temporalmente (comentario `TEMP-RED-CHECK`) el bloque de reglas de `inscripciones` en `firestore.rules` para confirmar RED del test de reglas, y se cortó antes de restaurarlo. Todo el código/tests de esta fase ya estaba escrito y completo; lo único roto era la regla de Firestore, dejada ausente. Esta revisión verificó cada pieza contra el código real (no contra los checkmarks previos, que ya estaban todos en `[ ]` pese a que el trabajo subyacente existía), restauró la regla, y corrió los tests reales (unitarios + emulador) para confirmar el estado GENUINO. Se encontró y corrigió además un bug real en la regla restaurada: ver 0.22.

- [x] 0.1 Crear `models/academico/inscripcion.ts` — tipo `InscripcionEjecucionPrograma` (estudianteId, ejecucionProgramaId, tenantId, estado, fechaInscripcion, inscritoPorUid). Sin test (solo tipos). Verificado: completo, coincide con `design.md`.
- [x] 0.2-0.9 RED→GREEN: `inscripcionRepository.ts`+`.test.ts` — `matricular/retirar/listarPorEjecucion/estaInscritoEnEjecucion` sobre `ejecucionesPrograma/{id}/inscripciones`, patrón mock de `programaRepository.ts`. Verificado: 9/9 tests pasan (`npx jest servicios/academico/inscripcionRepository.test.ts`).
- [x] 0.10-0.13 RED→GREEN: `inscripcionService.ts`+`.test.ts` — puras `estaInscrito()`, `sugerirEstudiantesPorAtributo()` (sugerencia UI, no valida pertenencia). Verificado: 6/6 tests pasan.
- [x] 0.14-0.19 RED→GREEN: `MatricularEstudiantesModal.tsx`+`.test.tsx` — sugerencia pre-marcada por grado/grupo, selección manual editable, confirmar persiste solo lo seleccionado, cancelar no persiste nada. Verificado (en su momento): 5/5 tests pasan.
  > **⚠️ Desactualizado (confirmado 2026-07-19, ver `KNOWN_ISSUES.md` y `CIERRE CENTRO DE ESTUDIOS.md` §13.0).** Este modal fue **descartado por decisión de producto el 2026-07-13** (dos días después de la matrícula automática de 0.10-0.13) y su archivo físico eliminado en el commit `ada44a8` — hoy no existe ningún modal de matrícula manual en el repo. `vistas/admin/AsignacionesView.test.tsx:1207-1222` tiene un describe explícito `'Rediseño: se elimino Asistencia a Clase en Vivo'` confirmando el retiro. Los checkboxes de arriba describen correctamente lo que se construyó Y VERIFICÓ en su momento, pero el estado actual del código ya no lo tiene — la matrícula real hoy es 100% automática server-side (`perteneceAEjecucion`), sin ninguna UI manual.
- [ ] ~~0.20 Modificar `vistas/admin/AsignacionesView.tsx` (~994-1020) — acción "Matricular estudiantes" por `EjecucionPrograma`, monta el modal.~~ **Revertido junto con el descarte del modal (ver nota arriba) — no existe hoy.**
- [ ] ~~0.21 Test `AsignacionesView.test.tsx` — botón abre modal con la ejecución correcta.~~ **Removido junto con el modal — reemplazado por el test de retiro citado arriba.**
- [x] 0.22 Regla Firestore `ejecucionesPrograma/{e}/inscripciones/{estudianteId}` en `firestore.rules` (junto a línea 244). **REPARADO en esta revisión**: la regla estaba completamente AUSENTE (comentario `TEMP-RED-CHECK` sin restaurar tras el corte de sesión). Restaurada según `design.md`. Además se encontró y corrigió un bug real de la especificación original: combinar `allow create, delete` en una sola condición que lee `request.resource.data.*` falla en `delete` porque `request.resource` es `null` en un borrado (Firestore Security Rules) — confirmado por el emulador (`Null value error. for 'delete' @ L254`). Fix: se separó `allow create` (con el chequeo `estudianteId`/`tenantId` del doc) de `allow delete` (solo rol+tenant), mismo patrón que ya usa este archivo para `asignaciones`.
- [x] 0.23-0.24 RED→GREEN emulador: `firestore-rules.behavior.test.js` — alta/baja instructor mismo tenant OK; cross-tenant y rol no autorizado (Estudiante/Tutor) denegado; `estudianteId`/`tenantId` del doc deben matchear la ruta. Verificado contra emulador real (`firebase emulators:exec --only firestore`): 33/33 tests de `firestore-rules.behavior.test.js` + `firestore-rules.security.test.js` en verde (incluye los 8 casos de `inscripciones` en líneas 502-631 del test file, más el fix del bug de 0.22).
- [x] 0.25 `npm test -- --runInBand` Fase 0 en verde. **GATE — cumplido.** Unitarios Fase 0 (repo+service+modal+AsignacionesView): 51/51 pass. Regresión completa del proyecto (`npx jest --runInBand`, 994 tests): 9 suites con 42 fallos preexistentes, ninguno relacionado a Fase 0 ni causado por este fix (afectan `jornadaContextService.test.ts`, `AsignarMaterialWizard.test.tsx`, `pagosApi.complementaria.test.ts` — módulos de otro WIP no tocado por este change; `firestore.rules` no es leído por Jest, solo por los tests del emulador).

## Phase 1: Callable de asistencia (bloqueado por Fase 0 verde)

**Nota de cierre (esta revisión)**: implementado con TDD RED→GREEN real, 3 ciclos incrementales (validaciones auth/rol/tenant/estado → pertenencia contra roster → toggle entrada/salida) verificados uno por uno contra `functions/academico/asistencia.js`. Regla de Firestore agregada siguiendo el protocolo seguro (test primero, sin remover ningún bloque existente) — RED confirmado por el catch-all deny-all antes de agregar la regla, GREEN confirmado contra el emulador real. Detalle completo en el reporte de progreso (Engram `sdd/clase-en-vivo-checkin-trigger-agenda/apply-progress`, sección Fase 1).

- [x] 1.1 Crear `models/academico/asistencia.ts` — tipo `RegistroAsistencia` (estudianteId, horaEntrada, horaSalida?, minutosAsistidos?). Sin test.
- [x] 1.2-1.7 RED→GREEN: `asistencia.js`+`.test.js` — rechazo unauthenticated, rol no autorizado, tenant mismatch, `jornada.estado!=='en_curso'` (+ jornada inexistente). Mocks `makeFirestore`/`makeContext` (node:test, mismo runner que ya usa `asignaciones.test.js` en este repo — ver Issues del reporte de progreso sobre cómo leer su resultado real). 6/6 tests verdes.
- [x] 1.8-1.9 RED→GREEN: rechazo por NO-pertenencia — estudiante existe pero sin inscripción en `jornada.ejecucionProgramaId` (comparte grado/grupo con otra sección), `exists()` contra roster de Fase 0; caso adicional fail-closed sin roster. 8/8 tests verdes (acumulado).
- [x] 1.10-1.15 RED→GREEN: toggle server-side — 1er escaneo = `horaEntrada` (check-in), 2do = `horaSalida`+`minutosAsistidos` (check-out, cálculo verificado con reloj fijo), 3ro rechazado. 12/12 tests verdes (acumulado).
- [x] 1.16 Registrar `exports.registrarAsistenciaJornada` en `functions/index.js`.
- [x] 1.17 Regla `asistencias` en `firestore.rules` (junto al bloque `auditoria`): `allow write: if false`, `allow read: authenticated+tenant`.
- [x] 1.18-1.19 RED→GREEN emulador: deny write cliente (incluso instructor mismo tenant), allow read mismo tenant, deny cross-tenant. RED real confirmado (1 test fallando por catch-all antes de agregar la regla) → GREEN tras agregarla.
- [x] 1.20 `npm test -- --runInBand` Fase 1 en verde. 976 pass / 27 fail preexistentes en 7 suites no relacionadas (`ModalImportacionMasiva`, `CentroEstudios`, `App.routing`, `FilaEstudiante`, `ModalRegistrarPago`, `jornadaContextService`, `pagosApi.complementaria` — ninguna toca asistencia/inscripción/reglas de jornadas). `functions/academico` (jest.config propio): 28/28 node:test verdes + 76/76 jest verdes. Reglas Firestore (emulador real): 36/36 verdes.
- [x] 1.21 **Gap real encontrado y corregido en auditoría independiente (esta revisión)**: el callable `registrarAsistenciaJornada` autorizaba por rol (`isInstructor()`) pero NUNCA validaba `jornada.instructorId === auth.uid` — cualquier `Editor` ("maestro" en este dominio) del tenant podía marcar asistencia en la clase de OTRO maestro, violando `Módulo Clase en Vivo.txt` §12 ("Un maestro solo puede operar clases donde esté asignado", explícito y no ambiguo). No era una interpretación válida del "rol autorizado" de la spec — el propio repo ya tenía el precedente exacto en `validarJornada` de `functions/academico/asignaciones.js:50-57` (`jornada.instructorId !== uid` → `permission-denied`), citado por `design.md` Decisión 2 como patrón de referencia para este mismo callable. RED→GREEN real: 3 tests nuevos en `asistencia.test.js` (rechaza Editor no asignado; permite Admin y Asistente operar jornada de otro instructor, igual que ya permite `isInstructor()` en `firestore.rules`), fix en `asistencia.js` (`assertInstructorAsignado`, invocado tras validar `estado==='en_curso'`). SuperAdmin/Admin/Asistente conservan alcance de tenant completo (consistente con `design.md` Bloque B "Roles y permisos", línea 222, que ya establecía esta misma distinción para el filtro de UI de Fase 9 — este fix la aplica también en el callable de Fase 1, que es el gate real de autorización server-side). 15/15 tests de `asistencia.test.js` verdes tras el fix; 36/36 reglas Firestore sin cambios (no tocan esta lógica).
- [x] 1.22 **Hallazgo cosmético (no bloqueante)**: la edición previa de `firestore.rules` dejó mojibake (doble-encoding UTF-8→Latin1) en varios comentarios (`acadÃ©mico`, `â€”`, etc., líneas 56/194/203/212/221/229/266/272/302/312). No afecta la sintaxis ni evaluación de las reglas (los comentarios no se ejecutan, confirmado: las 36 pruebas del emulador pasan igual). Se deja documentado para limpieza de higiene en un commit futuro; fuera de alcance de esta auditoría corregirlo junto con lógica de negocio.

## Phase 2: Wiring cierre de jornada (bloqueado por Fase 1 verde)

**Nota de cierre (esta revisión)**: implementado con TDD RED→GREEN real. Durante la implementación se detectó que `vistas/admin/JornadasView.tsx`/`JornadasView.test.tsx` estaban siendo editados en paralelo por otro agente (Subtarea 12.5, auditoría rol/fuente/diff — fuera del alcance de este change). Se re-leyó el archivo inmediatamente antes de cada edición y se usaron `Edit` quirúrgicos sobre anclas no tocadas por esa Subtarea (el bloque del checkbox de asistencia y el estado `asistenciaRegistrada`), sin pisar ese trabajo concurrente. `firestore.rules` no requirió cambios en esta fase (la regla de lectura de `asistencias` ya existe desde la Fase 1: `allow read: authenticated+tenant`).

- [x] 2.1-2.4 RED→GREEN: `asistenciaRepository.ts`+`asistenciaService.ts` (+tests) — lectura subcolección `asistencias`, `contarCheckIns()`, `calcularMinutosAsistidos()` puras. Repositorio de solo lectura (el cliente nunca escribe esta subcolección, solo el callable server-side de Fase 1); helper de test `seedMockAsistencias` para simular check-ins ya registrados sin configurar Firebase. 4/4 tests verdes en `asistenciaService.test.ts`, 4/4 en `asistenciaRepository.test.ts`.
- [x] 2.5-2.6 RED→GREEN: `vistas/admin/JornadasView.tsx` (~117-119, 217-220) — `asistenciaRegistrada` viene de `contarCheckIns()>0` sobre un nuevo efecto que lee `asistenciaRepository.listarPorJornada(tenantId, jornadaId)`, no checkbox manual (removido); firma de `cerrarJornada()`/`marcarPendienteCierre()` sin cambios (siguen recibiendo el mismo booleano, ahora derivado en vez de manual).
- [x] 2.7 Test regresión: cierre sin check-ins igual que hoy — `muestra error si intenta cerrar sin check-ins de asistencia registrados` en `JornadasView.test.tsx`, con `asistenciaRepository` mock devolviendo `[]`.
- [x] 2.8 `npm test -- --runInBand` Fase 2 + `JornadasView` en verde. Suites nuevas/tocadas: `asistenciaService.test.ts` 4/4, `asistenciaRepository.test.ts` 4/4, `JornadasView.test.tsx` 12/12 (incluye 2 tests de auditoría de la Subtarea 12.5 concurrente, no relacionados a este change). Regresión completa del proyecto (`npx jest --runInBand`, 1024 tests): 993 pass / 28 fail preexistentes en 8 suites ya documentadas en Fases 0-1 (`vistas/CentroEstudios.test.tsx`, `components/ModalImportacionMasiva.test.tsx`, `App.routing.test.ts`, `components/FilaEstudiante.test.tsx`, `components/ModalRegistrarPago.test.tsx`, `servicios/academico/jornadaContextService.test.ts`, `servicios/pagosApi.complementaria.test.ts`, `components/academico/AsignarMaterialWizard.test.tsx`) — ninguna toca asistencia/inscripción/reglas de jornadas ni los archivos de esta fase.

## Phase 3: Rewire escaneo (bloqueado por Fase 1 verde)

**Nota de auditoría (esta revisión)**: un intento previo de `sdd-apply` se cortó por límite de sesión de la API justo antes de correr la tarea 3.7 (regresión), sin guardar progreso en Engram ni marcar ningún checkmark. Esta revisión auditó cada archivo dejado por ese intento contra el código real (no había ningún `[x]` previo del que desconfiar, pero tampoco ninguna garantía de que el trabajo estuviera completo) y encontró una **desviación real y justificada** del plan original de `design.md` (tabla "Fase 1+", fila `components/EscanerAsistencia.tsx`): el diseño original proponía cambiar las props de ese archivo de `sedeId`→`jornadaId,tenantId` directamente. Investigación de código (grep de consumidores reales) confirmó que `EscanerAsistencia.tsx` tiene **3 consumidores de producción no relacionados a Clase en Vivo** (`vistas/GestionClase.tsx:215`, `vistas/MiPerfil.tsx:215`, `vistas/PerfilTutor.tsx:145`, todos con el flujo de guardería/entrega a tutores vía `api.registrarEntrada`) — cambiarle las props tal como decía el plan original los habría roto a los tres. El agente cortado (deducido por el código dejado, no por ningún reporte) optó en su lugar por: (a) extraer la cámara/`BarcodeDetector` a un componente genérico nuevo `components/academico/EscanerQR.tsx` (sin lógica de negocio, solo decodifica el QR y delega `estudianteId` a un callback), (b) dejar `EscanerAsistencia.tsx` con su interfaz pública intacta (`sedeId`/`onClose`) como wrapper delgado de `EscanerQR` que preserva el flujo de guardería, y (c) crear un consumidor nuevo `components/academico/EscanerAsistenciaClase.tsx` que envuelve `EscanerQR` con el callable `registrarAsistenciaJornada` (Fase 1) para Clase en Vivo. Verificado contra el código real, no contra ninguna nota dejada por el intento anterior:
- [x] 3.1-3.3 (reinterpretado, ver nota arriba) RED→GREEN: extracción de `components/academico/EscanerQR.tsx` (cámara/`BarcodeDetector` genérico, `onDetectarEstudiante`/`onClose` configurables) + refactor de `components/EscanerAsistencia.tsx` a wrapper delgado (`sedeId`/`onClose` sin cambios, sigue llamando `api.registrarEntrada`) + `components/academico/EscanerAsistenciaClase.tsx` (nuevo, `tenantId`/`jornadaId`/`onClose`/`onRegistrado`, llama `servicios/academico/asistenciaClaseService.ts::registrarAsistenciaClase` → callable `registrarAsistenciaJornada`, no cierra el escáner tras un escaneo exitoso para permitir múltiples estudiantes en secuencia). Tests verificados: `components/EscanerAsistencia.test.tsx` (suite preexistente, NO modificada por el refactor — sigue verde tal cual, confirma que el comportamiento externo no cambió) 6/6 pass; `components/academico/EscanerAsistenciaClase.test.tsx` (nuevo) 6/6 pass; `servicios/academico/asistenciaClaseService.test.ts` (nuevo) 3/3 pass.
- [x] 3.4-3.6 RED→GREEN: `vistas/ClaseEnVivoView.tsx` (rewrite in-place, deja de importar el Sistema B `claseEnVivoApi.ts`/`asistenciaQrApi.ts`) — recibe `jornadaId` real por parámetro de ruta (`useParams`, con fallback a prop `jornadaId` y al `jornada.id` deprecado del call site actual de `App.tsx:334`, que Fase 4 reemplaza), carga la jornada vía `jornadaRepository.listarJornadasPorTenant`, monta `EscanerAsistenciaClase` con `tenantId`/`jornadaId` reales, lista check-ins en vivo vía `asistenciaRepository.listarPorJornada` con refresco tras cada registro (`onRegistrado`), y cubre los casos "no encontrada" y "no está en_curso". Test verificado: `vistas/ClaseEnVivoView.test.tsx` (nuevo) 6/6 pass.
- [x] 3.7 `npm test -- --runInBand` Fase 3 en verde. Suites de esta fase: 4/4 (`EscanerAsistencia.test.tsx`, `EscanerAsistenciaClase.test.tsx`, `asistenciaClaseService.test.ts`, `ClaseEnVivoView.test.tsx`), 21/21 tests. Regresión `npx jest components/ servicios/academico/ vistas/ --runInBand`: 80/85 suites, 654/677 tests pass (20 fallos preexistentes en 5 suites — `CentroEstudios.test.tsx`, `ModalImportacionMasiva.test.tsx`, `FilaEstudiante.test.tsx`, `ModalRegistrarPago.test.tsx`, `jornadaContextService.test.ts` — ninguna toca escaneo/asistencia/Clase en Vivo, ya documentadas como preexistentes en el reporte de Fase 0). Regresión completa del proyecto (`npm test -- --runInBand`) corrida como verificación adicional, ver resultado en Engram `apply-progress`. `firestore.rules` sin cambios en esta fase (confirmado, Fase 3 es solo de componentes UI): sin marcadores `TEMP-*` ni ediciones a medio hacer.

## Phase 4: Ventana dinámica + trigger de Agenda (bloqueado por Fase 3 verde)

**Nota de cierre (esta revisión)**: implementado con TDD RED→GREEN real. Detalle completo (deviations, decisiones de tie-break, gap documentado sobre "permiso administrativo explícito") en el reporte de progreso (Engram `sdd/clase-en-vivo-checkin-trigger-agenda/apply-progress`, sección Fase 4).

- [x] 4.1-4.3 RED→GREEN: `ventanaClaseEnVivoService.ts`+test — `calcularVentanaClaseEnVivo(jornadas, ahoraIso)` pura, ventana `[horaInicio-15min, horaFin+15min]` (cierre anclado a `horaFin`, no a `horaInicio` — corrección de `design.md`, valores locales por ahora, se centralizan en Fase 7); fechas fijas (jornada 10:00-11:00 → activa a las 09:45 y 11:14, inactiva a las 09:44 y 11:16, spec scenarios "Ventana abierta hasta horaFin+15"/"Ventana cerrada tras horaFin+15"). Tie-break de 2+ jornadas activas simultaneas: elige la mas proxima a `ahoraIso` (diferencia absoluta de minutos), documentado como resolucion minima de Fase 4 (Bloque A), no el selector completo de Bloque B/Fase 9. Se agrego ademas `estaJornadaEnVentana` (helper puro exportado, reusado por `Horarios.tsx` en 4.7). Verificado: 9/9 tests pasan (`npx jest servicios/academico/ventanaClaseEnVivoService.test.ts`).
- [x] 4.4-4.5 RED→GREEN: `hooks/useVentanaClaseEnVivo.ts`+test — polling 60s + memo sobre el servicio puro; filtra jornadas por rol (Editor solo `instructorId===uid`, Admin/SuperAdmin/Asistente ven todas del tenant) reusando el mismo criterio ya aplicado server-side por el callable de la Fase 1 (`design.md` "Roles y permisos"). Verificado: 5/5 tests pasan (`npx jest hooks/useVentanaClaseEnVivo.test.ts`).
- [x] 4.6 Modificado `App.tsx` — quitado el placeholder `showClaseEnVivo=true`/`setInterval` inline; `BarraLateral` usa `useVentanaClaseEnVivo()`, el link "Clase en Vivo" solo aparece con `jornadaActiva` real y navega a `/clase-en-vivo/${jornadaActiva.id}`; rutas `/clase-en-vivo/:jornadaId` y `/clase-en-vivo` (sin id) montan `ClaseEnVivoView` sin props demo (`jornada={{id:'jornada-demo',...}}` eliminado).
- [x] 4.7 Modificado `vistas/Horarios.tsx` — botón "Iniciar Clase en Vivo" sobre `ClaseAcademicaAgenda` cuando `estaJornadaEnVentana(...)`, navega via `useNavigate()` a `/clase-en-vivo/${clase.jornadaId}` (jornadaId real, no `jornada-demo`); oculto para bloques comerciales, clases atenuadas (canceladas/reprogramadas) y fuera de ventana; visible solo para Admin/SuperAdmin/Editor/Asistente. **Deviation documentada** (ver reporte de progreso): `ClaseAcademicaAgenda.id` en `agendaAcademicaService.ts` es la clave de agrupacion (`bloqueRecurrenteId` en bloques recurrentes), NO el id real de la `JornadaInstruccion` de la proxima ocurrencia — se agrego el campo `jornadaId` (aditivo) exponiendo `proxima.id` real, necesario para navegar con un jornadaId operable. No estaba en la tabla de `design.md` (que no listaba `agendaAcademicaService.ts` entre los archivos de Fase 4), pero es necesario por la forma real del codigo existente. Verificado: 11/11 tests pasan (`npx jest servicios/academico/agendaAcademicaService.test.ts`), 7/7 tests pasan (`npx jest vistas/Horarios.test.tsx`).
- [x] 4.8 Test: sin jornada activa se impide acceso o muestra estado vacío (spec scenario "Sin jornada activa") — cubierto por `useVentanaClaseEnVivo.test.ts` ("devuelve null sin jornada activa") + comportamiento ya existente de `ClaseEnVivoView.tsx` (Fase 3, sin cambios) que muestra "No se encontró la jornada solicitada" cuando no hay `jornadaId`/jornada resuelta.
- [x] 4.9 `npm test -- --runInBand` Fase 4 en verde. Suites nuevas/tocadas de esta fase: `ventanaClaseEnVivoService.test.ts` 9/9, `useVentanaClaseEnVivo.test.ts` 5/5, `agendaAcademicaService.test.ts` 11/11, `Horarios.test.tsx` 7/7, `ClaseEnVivoView.test.tsx` 6/6 (sin cambios, confirma que `App.tsx` no rompio el consumidor) — 38/38 tests. Regresión completa del proyecto (`npx jest --runInBand`, 1072 tests): 1042 pass / 27 fail preexistentes en 7 suites ya documentadas en fases previas (`CentroEstudios.test.tsx`, `ModalImportacionMasiva.test.tsx`, `App.routing.test.ts`, `FilaEstudiante.test.tsx`, `ModalRegistrarPago.test.tsx`, `pagosApi.complementaria.test.ts`, `jornadaContextService.test.ts`) — ninguna toca ventana/agenda/horarios/App.tsx/Clase en Vivo. `firestore.rules` sin cambios en esta fase (confirmado, Fase 4 es solo cliente: servicio puro, hook, Agenda, App.tsx).
- **Gap documentado (no implementado, sin base suficiente en `design.md`)**: `proposal.md` y `Módulo Clase en Vivo.txt` §3 mencionan "No permitir check-in ni check-out fuera de la ventana permitida, salvo que exista un permiso administrativo explícito", pero ni `design.md` ni `tasks.md` definen el mecanismo concreto (¿quién lo activa? ¿un rol especifico bypassa la ventana? ¿un flag por tenant/jornada?). No se inventó un mecanismo sin base de diseño — queda sin implementar en Fase 4 y debe resolverse explícitamente en Bloque B (candidato natural: Fase 7, que ya valida la ventana server-side) o en una decisión de diseño dedicada antes de esa fase.
- [x] 4.10 **Auditoría independiente (esta revisión)**: verificado contra el código real, no contra el reporte previo. Confirmado correcto: (a) fórmula de ventana `[horaInicio-15, horaFin+15]` en `ventanaClaseEnVivoService.ts` — cierre anclado a `horaFin` (línea 40), no a `horaInicio`, tests cubren los 4 bordes pedidos (09:44 fuera / 09:45 borde inclusivo dentro / 11:14 dentro / 11:16 fuera); (b) filtro por rol en `useVentanaClaseEnVivo.ts` coincide exactamente con `assertInstructorAsignado()` server-side de `functions/academico/asistencia.js:42-49` (mismo criterio `rol==='Editor' && instructorId!==uid`, confirmado `usuario.id===firebaseUser.uid` en `AuthContext.tsx`); en `Horarios.tsx` el gate `(esAdmin||esInstructor)` no es más permisivo porque el filtro `filtroInstructor` (preexistente, no tocado por Fase 4) ya restringe a un Editor a solo sus propias clases sin UI para cambiarlo; (c) `ClaseAcademicaAgenda.jornadaId` en `agendaAcademicaService.ts` expone `proxima.id`, que es el mismo id space que `jornadaRepository.listarJornadasPorTenant` lee de `tenants/{t}/jornadas/{id}` y que el callable de Fase 1 espera como `jornadaId` — navegación end-to-end confirmada leyendo `Horarios.tsx:268` (`navigate(/clase-en-vivo/${claseAcademica.jornadaId})`); (d) gap de "permiso administrativo explícito" razonable de dejar sin implementar, sin decisión de diseño pasada por alto. **Bug real encontrado y corregido**: el test "flaky" reportado (`Horarios.test.tsx`, describe "Fase 4: trigger de Clase en Vivo") derivaba sus fixtures de horario con reloj real (`new Date()` en el momento de import del archivo) vía `formatoHoraUtc` (solo HH:MM, sin fecha) emparejado con una `fechaHoy` fija — si el offset cruzaba las 00:00 UTC, el HH:MM resultante correspondía al día SIGUIENTE pero quedaba emparejado con la fecha de HOY, rompiendo `estaJornadaEnVentana` en una ventana de ~40min/día alrededor de medianoche UTC. Bug de construcción del test, no de `ventanaClaseEnVivoService.ts`/`Horarios.tsx` (código de producción sin cambios). Fix: la describe "Fase 4" ahora usa `jest.useFakeTimers()+setSystemTime(2026-06-06T12:00:00Z)` (mediodía UTC, lejos de cualquier borde de día) en vez de reloj real, mismo patrón ya probado en `useVentanaClaseEnVivo.test.ts`. Verificado: 7/7 tests de `Horarios.test.tsx` en verde, corridos 3 veces seguidas sin fallos. Regresión completa (`npx jest --runInBand`, 1094 tests): 1064 pass / 27 fail — **exactamente las mismas 7 suites preexistentes** ya documentadas en 4.9 (`CentroEstudios.test.tsx`, `ModalImportacionMasiva.test.tsx`, `App.routing.test.ts`, `FilaEstudiante.test.tsx`, `ModalRegistrarPago.test.tsx`, `pagosApi.complementaria.test.ts`, `jornadaContextService.test.ts`), ninguna relacionada a Clase en Vivo/Agenda. `App.routing.test.ts` confirmado no relacionado: sus 6 fallos son por imports de `construirUrlCallbackDrive`/`obtenerCodigoCallbackDrive`/`obtenerRutaInicioUsuario` que no existen como named exports en `App.tsx` (que solo tiene `export default App`) — feature de Google Drive OAuth callback sin terminar, ajena a este change.

## Phase 5: Archivo del Sistema B (bloqueado por Fase 3 y 4 verdes)

**Nota de cierre (esta revisión)**: diagnóstico contra el código real antes de tocar nada (no contra las líneas/rutas estimadas de `design.md`/`proposal.md`, que databan de antes de la reescritura de Fase 3). Dos desviaciones reales encontradas y documentadas abajo (5.1 y 5.5). `claseEnVivoApi.ts`/`asistenciaQrApi.ts`/sus tests/`firestore-rules.etapa8.test.js` resultaron estar **sin trackear en git** (`git status --porcelain` los listaba `??`, nunca committeados) — por eso no hubo `git mv` real: se borraron directo del árbol, y el rastro de qué existía y por qué se borró queda en esta nota + Engram `apply-progress` (no hay commit previo del que colgar un `git log` de esos archivos).

- [x] 5.1 **Desviación real**: `ClaseEnVivoView.tsx` original NO existía para archivar — confirmado que Fase 3 ya lo reescribió in-place (su propio comentario de cabecera dice explícitamente "El archivado formal del Sistema B ... es tarea de la Fase 5 ... Este archivo deja de importarlos"), tal como anticipó el batch de esta fase. `git mv` tampoco aplicó en sentido literal porque `servicios/claseEnVivoApi.ts`/`servicios/asistenciaQrApi.ts` (+ sus tests) estaban **sin trackear en git** (confirmado `git status --porcelain` → `??`, nunca committeados) — no hay historia que preservar con `mv`. Acción real: confirmado 0 consumidores de producción (grep `from ['"].*claseEnVivoApi['"]` / `asistenciaQrApi['"]` → solo sus propios tests) y borrados directamente del árbol: `servicios/claseEnVivoApi.ts`, `servicios/claseEnVivoApi.test.ts`, `servicios/asistenciaQrApi.ts`, `servicios/asistenciaQrApi.test.ts`.
- [x] 5.2 Eliminadas de `firestore.rules` las reglas huérfanas `clases_en_vivo`/`asistencias_jornada` (líneas reales 172-192 al momento de editar, incluye el header de sección — el rango original `176-191` de `tasks.md` no incluía el comentario de sección, que también se retiró para no dejar un encabezado sin contenido). Confirmado 0 código de producción escribe/lee esas dos colecciones (ni `claseEnVivoApi.ts`/`asistenciaQrApi.ts`, que eran funciones puras sin `firestore.collection(...)`, según `design.md`: "Sistema B nunca persistió en Firestore" — confirmado leyendo el código real, no solo citando el diseño). `git diff -- firestore.rules` corrido inmediatamente después del `Edit`: diff limpio, un solo bloque contiguo removido, sin marcadores `TEMP-*` ni edición a medio hacer.
- [x] 5.3 Eliminado `functions/test/firestore-rules.etapa8.test.js`. Confirmado que testeaba EXCLUSIVAMENTE `clases_en_vivo`/`asistencias_jornada` (las 4 pruebas del describe "Etapa 8", pese a que el título menciona "Jornadas" no hay ningún caso sobre jornadas reales) y que no está referenciado por ningún script de `package.json` (`test:firestore-rules` corre explícitamente solo `firestore-rules.security.test.js` + `firestore-rules.behavior.test.js`; `functions/package.json`'s `test` corre `*.test.js` de la raíz de `functions/` + `asistente/*.test.js`, no `test/*.test.js`) ni por `.github/workflows/deploy.yml`. Archivo sin trackear en git (`??`), igual que 5.1.
- [x] 5.4 `ClaseEnVivoIntegracion.test.tsx`: **no existe en el árbol** (glob `**/*ClaseEnVivoIntegracion*` → 0 resultados) — ya estaba ausente antes de esta fase, nada que borrar. `claseEnVivoApi.test.ts`/`asistenciaQrApi.test.ts`: borrados junto con 5.1 (mismos archivos, un solo `rm`).
- [x] 5.5 **Desviación real de alcance** (difiere de `design.md`/`proposal.md`, que asumían los 4 tipos con 0 consumidores fuera de Sistema B — esa asunción no se sostuvo al verificar contra el código real): grep de cada tipo por separado (`\bJornadaAcademica\b`, `\bClaseEnVivo\b`, `\bEventoAsistenciaQr\b`, `\bAsistenciaJornada\b`) reveló que `JornadaAcademica` (usado también por `servicios/jornadasApi.ts`+test, `servicios/agendaManualApi.ts`+test, `tipos.test.ts`) y `AsistenciaJornada` (usado también por `servicios/progresoClaseApi.ts`+test) tienen consumidores **fuera de Sistema B y fuera del alcance de esta fase** — un clúster de código igualmente huérfano (`jornadasApi.ts`/`agendaManualApi.ts`/`cohortesApi.ts`/`progresoClaseApi.ts`, ninguno con consumidor de producción, solo sus propios tests) pero que NO es Sistema B (no lo menciona `proposal.md`/`design.md` en ningún lado) y no estaba en el batch asignado. Borrar esos dos tipos habría roto la compilación de 3 archivos fuera de mi alcance. Acción real: se borraron ÚNICAMENTE `ClaseEnVivo` y `EventoAsistenciaQr` de `tipos.ts` (confirmado 0 consumidores fuera de `claseEnVivoApi.ts`/`asistenciaQrApi.ts`, ya borrados) — `JornadaAcademica` y `AsistenciaJornada` se conservan intactos; se actualizó el comentario JSDoc de `AsistenciaJornada` (que citaba a `asistenciaQrApi`, ahora inexistente) para no dejar una referencia colgante a código borrado. **Hallazgo para un change futuro** (fuera de alcance, no accionado): el clúster `jornadasApi.ts`/`agendaManualApi.ts`/`cohortesApi.ts`/`progresoClaseApi.ts` es candidato a su propia auditoría de archivado — 0 imports de producción confirmados por grep, pero no es este change quien decide su destino.
- [x] 5.6 Grep repo-wide (`*.{ts,tsx,js,jsx}`) de `claseEnVivoApi|asistenciaQrApi|ClaseEnVivoIntegracion|clases_en_vivo|asistencias_jornada|\bClaseEnVivo\b|\bEventoAsistenciaQr\b` tras los borrados: 2 archivos con matches, ambos comentarios de prosa inertes (no imports/código ejecutable) — `tipos.ts` (la nota nueva de 5.5, documentando qué se archivó) y `vistas/ClaseEnVivoView.tsx` (comentario de cabecera de Fase 3, histórico, menciona los nombres de archivo en texto). `App.tsx`: 0 matches. 0 referencias ejecutables residuales.
- [x] 5.7 Ver Engram `apply-progress` sección Fase 5 para los números reales de `npm test -- --runInBand` (antes/después) y `npm run build`.

## Phase 6: E2E y verificación manual Bloque A (bloqueado por Fase 5 verde)

- [ ] 6.1 Extender `cypress/e2e/modulo-estudio-cierre-jornada.cy.ts` — matrícula (Fase 0) → trigger desde Agenda en ventana → check-in/check-out → cierre de jornada con asistencia real.
- [ ] 6.2 `npx cypress run` en verde para el spec extendido.
- [ ] 6.3 `npm test -- --runInBand` + `npm run test:coverage` + `npm run build` — regresión completa, 0 fallos nuevos.
- [ ] 6.4 Actualizar `design.md` con desviaciones encontradas durante apply (patrón de fases previas del proyecto).
- [ ] 6.5 **GATE**: correr `sdd-verify` de Fases 0-6. Bloque B (Fase 7+) no arranca hasta que este verify confirme 0 regresiones.

## Riesgo operativo Bloque A (recordatorio, no técnico)

`EjecucionPrograma`s activas hoy no tienen roster. Hasta completar 0.20/0.21 (matrícula manual) para cada una, el callable de Fase 1 rechaza a TODOS los estudiantes por diseño (fail-closed). No lanzar Fase 1 en producción sin comunicar este paso operativo.

---

# Bloque B — Funcionalidad completa (Fases 7-15)

**GATE explícito**: nada de esta sección arranca sin `sdd-verify` en verde de Fases 0-6 (tarea 6.5).

## Phase 7: Constantes centralizadas cross-runtime + validación de ventana server-side (bloqueado por GATE Bloque A→B)

- [ ] 7.1 Modificar `constantes.ts` — agregar `LIVE_CLASS_OPEN_BEFORE_MINUTES=15`, `LIVE_CLASS_CLOSE_AFTER_MINUTES=15`.
- [ ] 7.2 Crear `functions/academico/constantesClaseEnVivo.js` (CJS) — mismos valores, comentario cruzado hacia `constantes.ts`.
- [ ] 7.3 Test de paridad: un test Jest (cliente) + un test `node:test` (Functions) asertando 15/15 en cada archivo; comentario de "actualizar el hermano si cambia".
- [ ] 7.4 Modificar `ventanaClaseEnVivoService.ts` — consumir `constantes.ts` en vez de los valores locales de Fase 4.
- [ ] 7.5-7.8 RED→GREEN: `asistencia.js`+test — importar `constantesClaseEnVivo.js`; rechazar check-in/check-out fuera de `[horaInicio-15,horaFin+15]` (`failed-precondition`, aunque el cliente oculte el botón); aceptar dentro de ventana.
- [ ] 7.9 `npm test -- --runInBand` Fase 7 en verde.

## Phase 8: Check-in/check-out completos (bloqueado por Fase 7 verde)

- [ ] 8.1 Extender `models/academico/asistencia.ts` — `RegistroAsistencia` +`checkedInBy`, `teacherId`, `venueId`, `status`, `isLate`, `minutesLate`, `checkedOutBy`, `attendanceStatus`, `notificationStatus` (aditivo).
- [ ] 8.2-8.4 RED→GREEN: `asistencia.js` rama entrada — `isLate`/`minutesLate` (caso `horaInicio=10:00`, check-in 10:12 ⇒ `isLate=true`,`minutesLate=12`; caso a tiempo ⇒ `isLate=false`).
- [ ] 8.5-8.6 RED→GREEN: `asistencia.js` rama salida — `durationMinutes` (10:12→10:57 ⇒ 45), `attendanceStatus='completa'`.
- [ ] 8.7-8.8 RED→GREEN: persistir `checkedInBy`/`checkedOutBy`/`teacherId`/`venueId` en ambas ramas.
- [ ] 8.9-8.10 RED→GREEN: `asistenciaService.ts` — puras de soporte para métricas (§11: horas acumuladas por sesión, conteo de tardíos).
- [ ] 8.11 `npm test -- --runInBand` Fase 8 en verde.

## Phase 9: Selector de clase múltiple (bloqueado por Fase 8 verde; corre en paralelo con Fases 10, 11, 12)

- [ ] 9.1-9.4 RED→GREEN: `ventanaClaseEnVivoService.ts` — `calcularVentanaClaseEnVivo`→`calcularJornadasEnVentana` (retorna 0..N), casos 0/1/N jornadas.
- [ ] 9.5-9.7 RED→GREEN: `filtrarJornadasPorPermiso` — Editor (maestro) solo `instructorId===uid`; Admin/SuperAdmin/Asistente ven todas del tenant.
- [ ] 9.8-9.9 RED→GREEN: `hooks/useVentanaClaseEnVivo.ts` — adaptar a array de resultados.
- [ ] 9.10-9.12 RED→GREEN: `components/academico/SelectorClaseActiva.tsx`+test — se muestra con 2+ resultados, usuario elige, se monta la jornada elegida.
- [ ] 9.13 `npm test -- --runInBand` Fase 9 en verde.

## Phase 10: Notificación a acudientes vía Meta WhatsApp Cloud API (bloqueado por Fase 8 verde; corre en paralelo con Fases 9, 11, 12)

**Decisión de producto (ver `proposal.md`/`design.md` Decisión 13, reemplazada)**: el usuario decidió construir un servicio real de WhatsApp automatizado (Meta WhatsApp Cloud API, server-side) en vez del mecanismo manual client-side (`wa.me`) o de la extensión de navegador no oficial. **EJECUTADO fuera de orden por pedido explícito del usuario, ver detalle en `CIERRE CENTRO DE ESTUDIOS.md` módulo 13** — un agente hermano implementó estas tareas con TDD RED/GREEN real en paralelo a la actualización de estos artefactos SDD.

- [x] 10.1 Modificar `tipos.ts:285-306` — `TipoNotificacion.ClaseFinalizada` (aditivo); `NotificacionHistorial.estado?`/`.intentos?`/`.errorMensaje?` (aditivos, opcionales — no rompen `useGestionNotificaciones.ts`).
- [x] 10.2 Confirmar dependencia `axios` ya presente en `functions/package.json` (`^1.13.5`) — sin instalación nueva.
- [x] 10.3-10.4 RED→GREEN: crear `functions/notificaciones/whatsappCloudApi.js` (carpeta nueva `functions/notificaciones/`, consistente con `functions/academico/`, `functions/asistente/`) — `enviarWhatsAppCloudApi({telefono, plantilla, parametros})`; test de éxito: mock `axios.post` responde 200 → `{exito:true, mensajeId}`.
- [x] 10.5-10.6 RED→GREEN: test de error de red/API — mock `axios.post` rechaza o responde 4xx/5xx de Meta → `{exito:false, error}`, sin excepción no controlada.
- [x] 10.7-10.8 RED→GREEN: test de credenciales faltantes — `WHATSAPP_CLOUD_API_TOKEN`/`WHATSAPP_CLOUD_API_PHONE_NUMBER_ID` no configurados → `{exito:false, error}` sin llamar a la red.
- [x] 10.9 Declarar secrets Firebase Functions v2 vía `defineSecret('WHATSAPP_CLOUD_API_TOKEN')` y `defineSecret('WHATSAPP_CLOUD_API_PHONE_NUMBER_ID')` en `whatsappCloudApi.js`; endpoint `POST https://graph.facebook.com/v20.0/{WHATSAPP_CLOUD_API_PHONE_NUMBER_ID}/messages`.
- [x] 10.10-10.11 RED→GREEN: wiring del callable de check-out (`functions/academico/asistencia.js`, rama salida) — invoca `enviarWhatsAppCloudApi` server-side tras `checkOutTime` exitoso, plantilla `clase_finalizada_notificacion`, parámetros (nombre estudiante, hora salida, sede, nombre de clase, `.txt` §8); persiste `estado`/`mensajeId`/`errorMensaje` en `NotificacionHistorial`.
- [x] 10.12-10.13 RED→GREEN: sin acudiente registrado → `NotificacionHistorial.estado='no_aplica_sin_acudiente'`, no se invoca `enviarWhatsAppCloudApi`, check-out sigue exitoso (no bloquea).
- [x] 10.14-10.15 RED→GREEN: falla de envío → `estado='fallida'`, `errorMensaje` persistido, reintento controlado disponible (no automático en loop).
- [x] 10.16 Confirmar si `whatsappCloudApi.js` requiere registro en `functions/index.js` (función invocable directa) o si queda como módulo interno consumido solo por el callable de asistencia — documentar la decisión tomada.
- [x] 10.17 `vistas/ClaseEnVivoView.tsx` — reflejar `estado==='fallida'` con acción de reintento en UI (el envío ya no es una acción client-side que el operador dispare manualmente, la UI solo muestra estado y permite reintentar).
- [x] 10.18 `npm test -- --runInBand` Fase 10 en verde.

### Ítem operativo (no-código, checklist manual del usuario — bloqueante solo para el funcionamiento real, no para el desarrollo)

- [ ] 10.19 **[OPERATIVO — acción manual en Meta, no automatizable por código]**: crear/verificar cuenta WhatsApp Business en Meta for Developers; obtener Access Token + Phone Number ID; configurar los secrets vía `firebase functions:secrets:set WHATSAPP_CLOUD_API_TOKEN` y `firebase functions:secrets:set WHATSAPP_CLOUD_API_PHONE_NUMBER_ID`; pre-aprobar la plantilla `clase_finalizada_notificacion` en Meta Business Manager (parámetros: nombre del estudiante, hora de salida, sede, nombre de la clase) — WhatsApp exige plantillas pre-aprobadas para mensajes que el negocio inicia fuera de la ventana de 24hs de conversación activa. Sin este paso, `enviarWhatsAppCloudApi` retornará `{exito:false, error}` en producción real aunque el código y los tests (mockeados) estén en verde.

## Phase 11: Checkpoint de materiales, incluye fix de bug pre-existente (bloqueado por Fase 8 verde; corre en paralelo con Fases 9, 10, 12)

- [x] 11.1-11.2 **Fix de bug pre-existente descubierto durante el diseño (no introducido por este change)**: RED→GREEN — `functions/academico/asignaciones.js:89` fuerza `estado:'publicada'` incondicionalmente en cada `set()`, revirtiendo silenciosamente una asignación `cerrada`/`vencida`. Test: "actualizar checkpoint sobre asignación `cerrada` no debe revertir su `estado`". Fix: preservar `estado` existente en updates (doc ya existe), aplicar el default `'publicada'` solo en creación real. **EJECUTADO fuera de orden por pedido explícito del usuario, ver detalle en `CIERRE CENTRO DE ESTUDIOS.md` módulo 13** (agente hermano aplicó el fix con TDD RED/GREEN real, en paralelo a la actualización de estos artefactos SDD).
- [ ] 11.3 Extender `models/academico/asignacion.ts` — campos aditivos `checkpointInicio?`/`checkpointAvance?`/`checkpointNota?`/`checkpointCierre?`.
- [ ] 11.4-11.6 RED→GREEN: `checkpointMaterialService.ts`+test — transición sub-fase inicio (3 valores), sub-fase avance (7 valores), cálculo de % de cobertura para el resumen de cierre.
- [ ] 11.7-11.10 RED→GREEN: `components/academico/CheckpointMaterialesClase.tsx`+test — flujo guiado de las 3 sub-fases (checkboxes/selects, sin prompt libre), empty state "clase sin materiales asignados", persiste vía `actualizarAsignacion()` existente sin bloquear el check-in.
- [ ] 11.11 `npm test -- --runInBand` Fase 11 en verde.

## Phase 12: Observaciones rápidas grupales (bloqueado por Fase 8 verde; corre en paralelo con Fases 9, 10, 11)

- [ ] 12.1 Crear `models/academico/observacion.ts` — `ObservacionRapidaClase` + `CategoriaObservacionClase` (8 categorías fijas).
- [ ] 12.2-12.3 RED→GREEN: `observacionRepository.ts`+test — crear sobre subcolección `jornadas/{j}/observaciones` (patrón mock-cuando-no-configurado).
- [ ] 12.4 Regla Firestore `jornadas/{j}/observaciones/{id}` (junto a línea 259): `allow create,read: if isInstructor()+tenant`, `allow update,delete: if false` (patrón `auditoria`).
- [ ] 12.5-12.6 RED→GREEN emulador: allow create/read mismo tenant, deny update/delete.
- [ ] 12.7-12.8 RED→GREEN: `ObservacionesRapidasClase.tsx`+test — selección de categoría + nota corta (límite 240 caracteres, default de implementación pendiente de validar con negocio, ver Open Question de `design.md`).
- [ ] 12.9 `npm test -- --runInBand` Fase 12 en verde.

## Phase 13: Estado derivado + ensamblado visual de las 5 secciones (bloqueado por Fases 9, 10, 11 y 12 verdes — ensambla lo que construyeron las tres)

- [ ] 13.1-13.6 RED→GREEN: `estadoClaseEnVivoService.ts`+test — un caso por fila del mapeo (`cancelled`, `closed`, `expired`, `in_progress`, `available`, `scheduled`); `cancelled` MUST tener prioridad sobre cualquier condición de tiempo.
- [ ] 13.7-13.9 RED→GREEN: `components/academico/ListaAsistenciaClase.tsx`+test — esperados/con check-in/pendientes/con check-out/tardíos, listener en vivo sobre `asistencias`, empty state "clase sin estudiantes esperados".
- [ ] 13.10-13.11 RED→GREEN: `components/EscanerAsistencia.tsx` — mensajes de éxito/error específicos (QR inválido, duplicado, fuera de tenant/clase) para la Sección B.
- [ ] 13.12-13.13 RED→GREEN: `vistas/ClaseEnVivoView.tsx` — ensamblar Secciones A-E completas: A) encabezado con `calcularEstadoClaseEnVivo`, B) `EscanerAsistencia`, C) `ListaAsistenciaClase`, D) `CheckpointMaterialesClase`, E) cierre con `ObservacionesRapidasClase`+resumen+confirmación → `jornadaService.cerrarJornada()`.
- [ ] 13.14 `npm test -- --runInBand` Fase 13 en verde.

## Phase 14: Casos especiales (matriz completa) + métricas consultables (bloqueado por Fase 13 verde)

- [ ] 14.1-14.2 RED→GREEN: `asistencia.js`+test — caso 15 de la matriz (§6): estudiante inactivo rechazado aunque esté en el roster (distinto del caso 3, activo pero de otra clase).
- [ ] 14.3 Test: caso 13 de la matriz — `filtrarJornadasPorPermiso` lee `instructorId` fresco de Firestore en cada consulta (sin denormalización), confirma que un cambio de maestro antes de abrir ventana se refleja automático.
- [ ] 14.4-14.5 Auditoría + completar gaps: confirmar que los 14 casos restantes de la matriz de `design.md` (doble QR, check-out sin check-in, cross-tenant, cámara no disponible, QR inválido, fuera de horario, sin materiales, sin estudiantes esperados, sin permiso, falla WhatsApp, clase sin cerrar, clase desactivada, sin acudiente) ya tienen test explícito de fases previas; agregar el test faltante si alguno no quedó cubierto.
- [ ] 14.6-14.8 RED→GREEN: `metricasClaseEnVivoService.ts`+test — asistencias, ausencias, tardíos, horas por sesión, materiales cubiertos/pendientes (puras, fechas fijas, sin dashboard).
- [ ] 14.9 `npm test -- --runInBand` Fase 14 en verde; tabular los 16 casos de la matriz con su test correspondiente (evidencia para `sdd-verify`).

## Phase 15: E2E y regresión completa Bloque A+B (bloqueado por Fase 14 verde)

- [ ] 15.1 Crear `cypress/e2e/clase-en-vivo-bloque-b.cy.ts` — flujo completo: matrícula → trigger Agenda → selector (si aplica) → check-in con retraso → checkpoint → check-out → notificación → observación → cierre.
- [ ] 15.2 `npx cypress run` en verde para el spec nuevo.
- [ ] 15.3 `npm run test:all` + `npm run build` — regresión completa Bloque A+B, 0 fallos.
- [ ] 15.4 **GATE final**: ejecutar `sdd-verify` de Bloque A+B completo; actualizar `design.md` con desviaciones encontradas durante apply.

## Riesgo operativo Bloque B (recordatorio, no técnico)

Fases 9-12 son paralelizables entre sí (todas solo dependen de Fase 8 verde) pero Fase 13 las necesita a las cuatro completas antes de ensamblar el flujo visual — no partir Fase 13 con piezas a medio construir.

**Notificación WhatsApp (Fase 10)**: el código y los tests de `whatsappCloudApi.js` no dependen de la configuración de Meta (mocks), pero el envío real en producción sí — la tarea 10.19 (checklist operativo en Meta for Developers/Business Manager) debe completarse antes de considerar Fase 10 "funcional end-to-end" en producción, aunque su código ya esté verde.
