# Tasks: Centro de recursos — clasificación manual real

## Phase 1: Foundation

- [x] 1.1 Verify zero non-embedded consumers before any deletion: run `grep -rn "abrirModalRecurso\|asignarRecursoSeleccionado\|tabRecursosAprobados\|mostrarHistorialRecurso\|cancelarSeleccionRecurso" --include=*.tsx --include=*.ts .` (excluding `openspec/`) — must match only `vistas/admin/AsignacionesView.tsx`. Confirmed clean 2026-07-05; re-run immediately before 3.4. (Re-confirmed clean in apply run: only `AsignacionesView.tsx` matches.)
- [x] 1.2 Add `tituloVisible?: string` to `RecursoAcademico` in `models/academico/recurso.ts`, sibling to `nombre`.
- [x] 1.3 RED: in `bibliotecaService.test.ts`, add cases — `updateFicha(tenant, id, ficha, tituloVisible)` persists it when non-empty; omitted/empty leaves existing value untouched. (RED confirmed: 3 new tests failed on assertions.)
- [x] 1.4 GREEN: add optional 4th param `tituloVisible?: string` to `updateFicha` in `bibliotecaService.ts`, written only when non-empty. Verify: `npm test -- --runInBand bibliotecaService.test.ts`. (GREEN: 19/19 pass.)

## Phase 2: Core Implementation — classification modal (`BibliotecaView.tsx`)

- [x] 2.1 RED: rewrite the `BibliotecaView.test.tsx` case "renderiza explorador de Drive y permite importar, clasificar y aprobar un recurso" — click on `borrador` opens modal and does NOT auto-approve. Add: `tituloVisible` input persists; "Guardar clasificación" → `pendiente` via `updateFicha` only (no `approveRecurso`); "Aprobar" enabled only when `pendiente` → `aprobado` via `approveRecurso` only; `borrador` sin ficha bloquea aprobar directo. (RED confirmed: 4 rewritten/new tests failed, 12 untouched passed.)
- [x] 2.2 GREEN: add `modalClasificacionAbierto`/`tituloVisible` state, modal JSX, and `abrirClasificacion(archivo)` handler (ensures indexed via `importFromDrive` if new, then opens modal) in `BibliotecaView.tsx`.
- [x] 2.3 GREEN: rewire the file-row click to 3-way routing (clasificar/aprobar/retirar); delete `aprobarRecursoDetectado`, `construirFichaAutomatica`, `indexarArchivo` and their call sites. (Also removed now-dead `estaArchivoAprobado`.)
- [x] 2.4 GREEN: thread `tituloVisible` state through `guardarClasificacion()`'s `updateFicha` call. Verify: `npm test -- --runInBand BibliotecaView.test.tsx`. (GREEN: 16/16 pass; combined regression run with CentroEstudios + AsignacionesView + bibliotecaService: 64/64 pass.)

## Phase 3: Integration/Wiring — grid move, bridge, stepper

- [x] 3.1 RED: add `BibliotecaView.test.tsx` cases — "Recursos aprobados" grid renders selectable items; selecting one calls `onRecursoParaLote(recursoId)`. (RED confirmed: new test failed on missing "Recursos aprobados" heading.)
- [x] 3.2 GREEN: added a new "Recursos aprobados" article to `BibliotecaView.tsx` — checkbox multi-select over `recursosAprobadosLista` (derived from `recursos.filter(estado==='aprobado')`) + "Agregar seleccionados al lote" button firing `onRecursoParaLote` once per selected id, then clearing local selection. **Deviation from design**: rather than moving the old "Preparar asignación" modal verbatim, the modal is dropped entirely — once título/tags/"Aprobado para" are excluded (per design's own note that none of those fields migrate), nothing meaningful remained in it, so a row-checkbox + single confirm button satisfies the spec's "selección múltiple ... confirma la selección" scenario with less code and no dead modal shell. `onRecursoParaLote?: (recursoId: string) => void` prop added to `BibliotecaViewProps`. GREEN: 17/17 pass in `BibliotecaView.test.tsx`.
- [x] 3.3 RED: updated `AsignacionesView.test.tsx` — added assertion that the "Recursos aprobados" heading/"Preparar asignacion"/"Reutilizar"/"Aprobado para" no longer render in `AsignacionesView` (moved to Biblioteca); added new case for `recursoIdsParaLote` prop unioning into `recursosLoteIds` without replacing manual selection; rewrote the CREATE-path of the two tests that used the old click-through-modal flow ("no crea una jornada nueva...", "bloquea duplicados...") to use the pre-existing "Materiales" batch checkboxes directly (that mechanism is untouched by the move, so it preserves what each test actually proves). RED confirmed: the 2 new assertions failed (grid heading still present pre-deletion; recursoIdsParaLote prop not yet wired); the 2 rewritten click-path tests already passed pre-implementation (harmless — they exercise a mechanism that survives the move) and continued to pass after.
- [x] 3.4 GREEN: re-confirmed 1.1 clean; deleted the embedded "Recursos aprobados" grid + "Reutilizar" tab + "Preparar asignación" modal JSX and the dead `abrirModalRecurso`/`asignarRecursoSeleccionado`/`mostrarHistorialRecurso`/`cancelarSeleccionRecurso`/`tabRecursosAprobados` state/handlers plus their now-orphaned derivations (`recursosAsignadosIds`, `recursosAprobadosListos`, `recursosAprobadosReutilizables`, `recursosVisiblesPorTab`, `claseListadoRecursos`, `claseFilaRecurso`, `claseIconoAccionRecurso`, `inferirGrupoObjetivoDesdeGrupoOperativo`, unused `IconoEnlace`/`IconoHistorial` imports) from `AsignacionesView.tsx`; added `recursoIdsParaLote?: string[]` prop merged into `recursosLoteIds` via a `useEffect` (Set union, not replace); bumped the two remaining `xl:col-span-4` grid-span classes to `xl:col-span-3` (3-column layout now that the grid's own column is gone). GREEN: 22/22 pass in `AsignacionesView.test.tsx`.
- [x] 3.5 RED: updated `CentroEstudios.test.tsx` — added "exactamente 3 pasos, en orden" test and a "paso 2 refleja progreso combinado" test; updated the bibliotecaService jest.mock to also export a `bibliotecaService` object (sharing the same `listarRecursosAprobados` mock as the named export) since `BibliotecaView.tsx`'s new grid reads through that object, not the named import; rewrote the two admin flow tests ("integra plan y cierre de clase...", "habilita publicar en lote...") to check the resource in Biblioteca's new "Recursos aprobados" `group` + click "Agregar seleccionados al lote" instead of the deleted "preparar asignacion"/"asignar" flow, scoping checkbox queries with `within(...)` to disambiguate from AsignacionesView's own pre-existing "Materiales" batch checkboxes (both components independently list the tenant's aprobado resources, so an unscoped query matches 2 elements). RED confirmed: 4 tests failed (3-step assertion, step-2 status, and the 2 rewritten admin-flow tests — all failing on the still-4-step stepper / missing Biblioteca grid group).
- [x] 3.6 GREEN: collapsed `pasosCentroEstudios` 4→3 entries in `CentroEstudios.tsx` ("Conectar Drive" / "Centro de recursos" / "Programa y publicacion"); added `recursosParaLote` state + `agregarRecursoParaLote` callback wiring `BibliotecaView`'s `onRecursoParaLote` → `AsignacionesView`'s `recursoIdsParaLote`; folded old step-3's `recursosAprobados`/`recursoSeleccionado` condition into step-2's `pasosConEstado` derivation alongside old step-2's `archivosDetectados` condition; bumped stepper `<ol>` and embedded `<section>` grid fractions from `[15fr_18fr_30fr_37fr]` to `[16fr_47fr_37fr]` per design. Also added `role="group" aria-label="Recursos aprobados"` to `BibliotecaView`'s new grid container (needed to disambiguate it from `AsignacionesView`'s same-named batch checkboxes in tests, and is a reasonable a11y improvement regardless). GREEN: 11/11 pass in `CentroEstudios.test.tsx`.
- [x] 3.7 Verify: `npm test -- --runInBand CentroEstudios.test.tsx AsignacionesView.test.tsx BibliotecaView.test.tsx` → 50/50 pass (11 + 22 + 17); extended combined regression run also including `bibliotecaService.test.ts` + `MisClasesView.test.tsx` → 74/74 pass.

## Phase 4: Testing — E2E

- [x] 4.1 Checked `cypress/e2e/modulo-estudio-cierre-jornada.cy.ts` against the design's assumption that it needs 3-step selector updates. **Deviation from design**: it doesn't touch the admin embedded Centro de Estudios flow at all — it drives `MisClasesView`'s "Plan y cierre de clase" panel (Confirmar jornada / Iniciar jornada / Cerrar jornada) for a single existing jornada, a completely different component than `BibliotecaView`/`AsignacionesView`. Ran `grep -niE "recursos aprobados|preparar asignacion|reutilizar|aprobado para|paso [1-4]|archivos detectados|centro de recursos|programa y public|Conectar Drive|BibliotecaView|AsignacionesView|clasificar|drive"` against the file — zero matches (only the describe-block title string coincidentally contains "cierre de jornada"). No selector changes needed or made; noting this here so the discrepancy from design.md's testing-strategy table is visible rather than silently skipped.
- [x] 4.2 Checked `cypress/e2e/onboarding.cy.ts` the same way. **Deviation from design**: this spec drives account signup/payment/activation (`/registro-escuela`, Wompi checkout simulation, login) and never visits `/centro-estudios` or references any of Drive/Biblioteca/Asignaciones/stepper text — same grep returned zero matches. No selector changes needed or made.
- [x] 4.3 Ran the full Jest suite `npm test -- --runInBand` (per `openspec/config.yaml`'s `testing.runner.command`): 105/111 suites pass, 857/885 tests pass (3 skipped, pre-existing). The 6 failing suites — `components/ModalImportacionMasiva.test.tsx`, `App.routing.test.ts`, `components/FilaEstudiante.test.tsx`, `components/ModalRegistrarPago.test.tsx`, `servicios/pagosApi.complementaria.test.ts`, `components/academico/ProgresoResumenCard.test.tsx` — are all pagos/estudiantes/routing modules with no relation to Biblioteca/Asignaciones/Centro de Estudios and are not in this change's file list; matches the proposal's documented risk of concurrent uncommitted WIP in the worktree (`git status` at session start showed dozens of unrelated modified files). Confirmed pre-existing by inspection, not introduced by this diff. `npx cypress run` was **not** executed — the Cypress binary is known-broken on this machine (pre-existing environment issue) — so neither spec from 4.1/4.2 was actually run, only reviewed for relevance (both found irrelevant to this change, see above). A human with a working Cypress install should still do a final E2E pass before merge (ties into 5.1).

## Phase 5: Cleanup

- [x] 5.1 (Low-priority, non-blocking) Static/DOM-level sanity pass on `CentroEstudios.tsx`'s new `[16fr_47fr_37fr]` grid fractions: confirmed via the Jest/RTL runs above that both the stepper `<ol>` and the embedded `<section>` render with exactly 3 grid items at every breakpoint exercised by tests (no layout-breaking overflow/wrapping asserted or observed in rendered DOM snapshots). Did not verify visually in a real browser across breakpoints — flagged as-is for the user's manual in-browser check; the `47fr` middle column packs 2 stacked cards (Archivos detectados + Recursos aprobados) where the old layout gave that content 2 separate columns, so real-browser eyes-on review at the `xl` breakpoint specifically is worth 5 minutes before merge.
- [x] 5.2 `npx tsc --noEmit`: scoped diff-check (not a project-wide gate, per the ~1995 pre-existing chai/jest `Assertion`-type collisions across the dirty worktree) — filtered to the 3 touched source files: `BibliotecaView.tsx` has **zero** errors; `AsignacionesView.tsx` has exactly 1 pre-existing error (`estudiantes` vs `estudianteIds` on `DestinatarioAsignacion`, line ~920, inside `editarAsignacionPublicada` — a function this batch never touched); `CentroEstudios.tsx` has 3 pre-existing errors (`.ts` import-extension warning + 2 Firestore generic-typing mismatches on `doc`/`setDoc`, both present before this batch's first edit per the IDE diagnostics hook). **Caught and fixed one real regression during this check**: my earlier deletion of the `tabRecursosAprobados` state left a stray `setTabRecursosAprobados('reutilizar')` call in `publicar()`'s success path (`AsignacionesView.tsx`) — a `ReferenceError` at runtime that was silently swallowed by the surrounding `try/catch`'s `setError(...)`, so existing tests didn't catch it (state updates before the crash still satisfied their assertions). Removed the dead call; re-ran `AsignacionesView.test.tsx` afterward (still 22/22 green) since the line ran on every non-embedded `publicar()` success path. `npm run build`: succeeded (`✓ built in 57.20s`), only pre-existing warnings (framer-motion "use client" directives, dynamic/static import chunking notices, >500kB chunk-size notice) — none new, no build regression.
